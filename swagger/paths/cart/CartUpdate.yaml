summary: Изменение корзины
description: Запрос предназначен для обновления основной информации о корзине. Возвращает обновлённую корзину.
tags:
  - Корзина
requestBody:
  $ref: '../../components/requestBodies/cart/UpdateCart.yaml'
responses:
  '200':
    description: Успех
    content:
      application/json:
        schema:
          allOf:
            - $ref: '../../components/schemas/responses/SuccessResponse.yaml'
            - type: object
              properties:
                data:
                  properties:
                    cart:
                      $ref: '../../components/schemas/orders/ShowCart.yaml'
