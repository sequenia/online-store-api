summary: Удаление позиции из корзины
description: Удаляет позицию из корзины по идентификатору позиции
tags:
  - Корзина
parameters:
  - $ref: '../../components/parameters/cartItems/CartItemId.yaml'
responses:
  '200':
    description: Успех
    content:
      application/json:
        schema:
          allOf:
            - $ref: '../../components/schemas/responses/SuccessResponse.yaml'
            - type: object
              properties:
                data:
                  properties:
                    cart:
                      $ref: '../../components/schemas/orders/ShowCart.yaml'
  '404':
    description: Ошибка "Не найдено"
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/responses/ErrorResponse.yaml'
        examples:
          CartItemNotFound:
            allOf:
              - $ref: '../../components/examples/responses/base/ErrorResponse.yaml'
              - summary: Позиция корзины не найдена
                value:
                  errorCode: cart_item_not_found
                  errorMessage: Товар уже удалён из корзины
