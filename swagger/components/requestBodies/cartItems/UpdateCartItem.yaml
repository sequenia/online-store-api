description: Данные, которые нужно задать в позицию корзины
content:
  application/json:
    schema:
      type: object
      properties:
        cartItem:
          type: object
          description: Информация о позиции в корзине
          properties:
            count:
              type: integer
              description: Количество
              example: 3
